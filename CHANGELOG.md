# Changelog

本檔記錄 NexFlow 的重要變更。格式依循 [Keep a Changelog](https://keepachangelog.com/zh-TW/1.1.0/)，版本遵循 [語意化版本](https://semver.org/lang/zh-TW/)。

## [未發布]

（尚無項目）

## [1.5.0] - 2026-08-30

這一版處理的是「我怎麼知道它有沒有在動」，以及兩個安靜到不像故障的故障。執行中的流程現在會在卡片、通知與長按閃電的清單裡同時現身；排程晚到十分鐘的原因是一個從來沒被要求過的權限；而 NexFlow 只要開著，就會把整支手機的 NFC 感應全部吃掉。

### 新增（Added）

- **執行中的流程看得見了，不管是誰啟動的**：卡片右下角在流程執行時轉成轉圈的「執行中」。這裡原本是點下執行鈕才亮三秒的計時器，所以側滑執行、時間到、地理圍欄、小工具啟動的流程一律不亮，而被權限擋下、根本沒開始的執行反而亮著。現在的來源是引擎自己用來擋重複執行的那份狀態，不是另一份會走樣的複本
  - 執行極快的流程會至少顯示 0.9 秒。三十毫秒跑完的流程是使用者真的要求過的一次執行，但指示器在一個影格內閃掉，和什麼都沒發生分不出來——而那正是這個指示器存在的理由
- **通知會說出正在跑哪一個流程**：常駐通知從「自動化執行中」變成「正在執行：<流程名稱>」，同時跑多個時顯示數量、展開列出名稱。背景觸發的流程原本只有一個容易錯過的 Toast 作為線索
- **長按右上角的閃電，列出目前執行中的流程**：從膠囊邊緣展開的浮層，每一列帶自己的轉圈指示。膠囊本身也會顯示執行中的數量，所以不長按也看得到

### 修正（Fixed）

- **排程到 9:00 的流程可能 9:10 才跑**：`SCHEDULE_EXACT_ALARM` 在 manifest 裡宣告了，但**沒有任何一處程式碼向使用者要過它**。targetSdk 33 以上的 App 在全新安裝（以及備份還原到新機）時這個權限預設是拒絕的，於是排程器安靜地退回不精確鬧鐘，被系統批次進 Doze 維護視窗——官方文件對不精確鬧鐘的保證是「一小時內」。從舊版一路升級上來的裝置會保留已授予的權限，這是為什麼同一版有人遇到、有人沒有
  - TIME 觸發器納入權限檢查：缺少精確鬧鐘時，流程卡片會亮出警告，設置向導把使用者帶到系統的「鬧鐘和提醒」頁
  - 使用者授予後，`SCHEDULE_EXACT_ALARM_PERMISSION_STATE_CHANGED` 會立刻重排所有 TIME 鬧鐘。鬧鐘只在排定當下決定精度，沒有這一步，授權後既有的鬧鐘會繼續遲到，直到使用者重新編輯流程或重開機
- **NexFlow 開著的時候，整支手機的 NFC 都失效**：`MainActivity` 無條件在 `onResume` 開啟 reader mode。reader mode 是獨占且全裝置生效的，`FLAG_READER_SKIP_NDEF_CHECK` 更直接關掉 NDEF 分派，所以感應網址標籤不會開啟瀏覽器、感應交通卡沒有反應——即使使用者一個 NFC 流程都沒有。現在只有「有啟用中的 NFC 流程且引擎正在執行」或「正在設定頁掃描標籤」時才佔用
  - 一併移除 manifest 的 `TAG_DISCOVERED` 攔截。它是標籤分派的最後手段，等於讓 NexFlow 成為所有其他 App 都不處理的標籤的萬用接收者（門禁卡、交通卡）；它在 API 37 也已經被標為 deprecated。NFC 觸發器本來就只在前景有效，從來不需要這個攔截
  - 掃描中讀到的標籤不再同時觸發流程。重新掃描「正在編輯的那個流程」所綁定的標籤，不應該把那個流程跑起來
  - 設定頁的掃描不再自己開關 reader mode。兩個持有者的結果是設定頁關閉掃描時，順手把 `MainActivity` 為了 NFC 觸發器而持有的那份也關掉了
- **音量動作的摘要顯示成「媒體：？」**：1.4.0 把音量從絕對階數（`level`）改存百分比（`volume_percent`），但流程詳情頁的摘要那一行仍然去讀舊的鍵。所有音量動作——包括用新版編輯器存的——都顯示成問號。摘要現在走與編輯器相同的正規化，1.4.0 之前寫的流程不必重新儲存也會顯示它實際會執行的百分比

## [1.4.0] - 2026-08-21

這一版補的是「跑完之後，到底發生了什麼」：執行記錄從一句成敗變成逐個動作的紀錄，HTTP 拿得回資料讓流程能依真實回應分支，而條件判斷不再對「從來沒有的值」下結論。同時把工具鏈與相依套件推到現行 stable，並讓測試從一層變四層、全部在 CI 上把關。

### 新增（Added）

- **執行記錄記下每一個動作，不只最後的成敗**：點一筆記錄就能看到每個動作的結果、耗時，依巢狀縮排，並標出條件走了哪一邊、重複到第幾輪、選單選了哪一項。「因為被停用而略過」也會記錄——在省略它的記錄裡，「這個動作是關的」和「根本沒跑到這裡」長得一模一樣，但要修的地方完全相反
  - Room 升到 v5，新增 `execution_steps`（純新增資料表，AutoMigration 仍涵蓋）。以 CASCADE 掛在 `execution_logs` 下，清理器只刪母記錄就不會留下孤兒
  - 單次執行上限 200 步，避免失控的 REPEAT 把資料庫撐大；**但失敗的那一步一定會留下**，那正是開啟記錄要找的東西
  - 結構性註記存成 token（`repeat:5`、`menu:Coffee`）在顯示時才在地化，換語言後舊記錄照樣讀得懂；錯誤訊息則原樣保留
  - 設定新增**「詳細執行紀錄」（預設關閉）**：另外記下每個動作的設定值解析結果。那些是流程的真實資料（標頭裡的 API 金鑰、簡訊內容、通知文字），所以由使用者自己選擇開啟；成敗紀錄不依賴它
- **HTTP 回應可以存進變數**，流程終於能依伺服器說的話分支
  - `response_var` 指定回應內容要放進哪個變數，`json_path` 用點路徑取出其中一個欄位（`main.temp`、`items.0.name`）；路徑對不上時會寫明是在哪一段停住的
  - 狀態碼一律發佈為 `{{__http_status__}}`。非 2xx **刻意不算失敗**——中斷執行等於拿掉狀態變數存在的意義
  - 自訂標頭，一行一個 `Name: Value`；缺冒號的行會直接指出來，而不是回一個難解的 401
- **`SET_VARIABLE` 支援四則運算**：`{{counter}} + 1` 存進去的是數字，計數器不再每跑一次就長三個字元。用 BigDecimal，所以 `0.1 + 0.2` 是 `0.3`、`1 + 1` 是 `2` 而非 `2.0`。運算子兩側**必須有空白**，這是「這是算式」與「這只是含有運算子的值」的分界，也是 `2026-08-10` 不會變成 `2008` 的原因
- **執行記錄可以複製**：展開的每一列可單獨複製解析後的值（實際打出去的網址、可以拿去搜尋的錯誤訊息），標題列可複製整趟執行的純文字逐字稿——依序、不折疊，每一輪重複都寫出來，巢狀變成縮排，狀態寫成 `[ok]`／`[FAIL]`／`[skip]`，貼到別處才不會只剩一堆圖示
- **AI 可以依「這是什麼 App」挑選，而不只是靠名字**：搜尋結果現在附上裝置自己知道的資訊——App 分類、它自己宣告的 `CATEGORY_APP_*` 角色，以及 manifest 裡的說明。查詢可以是一個種類（`MUSIC` 會列出音樂播放器），而名稱相符的仍然排在前面。多個結果都符合、或資訊不足以判斷時，提示詞要求模型回頭問使用者，而不是替他決定
- **主畫面小工具改成卡片格**：一列兩張卡，各自帶流程自己的圖示與顏色、名稱，以及是什麼在觸發它
  - 用 `LazyColumn` 疊成兩兩一列，而不是 `LazyVerticalGrid`——後者在 Glance 1.1.1 仍是 experimental，其 `GridCells.Adaptive` 又標了 `@RequiresApi(31)`，而 minSdk 是 30
  - 小工具狀態從 `"id|name,id|name"` 改存 JSON，順帶修好名字裡有逗號或直線（「Home, work」、「A|B」）的流程會被切成指向不存在項目的問題。舊編碼仍然讀得懂，已經放在桌面上的小工具不會空白
- **測試從一層變四層，全部在 CI 上把關**：Robolectric（不需實機的 Compose／資源測試）、Compose 預覽截圖測試（4 語系 × 深淺色，參考圖進 repo）、Gradle Managed Devices（api30／api36，instrumented 測試不再需要插著手機）、lint 作為會擋 PR 的工作。**Room migration 也終於有自動測試**

### 修正（Fixed）

- **條件會對「從來沒有的值」下判斷**：未解析的 `{{name}}` 被當成字面文字比較，`{{battery}} > 5` 在沒有人設定 battery 時回傳 true（因為 `{` 排序在 `5` 之上），流程就依一個它從來沒拿到的值行動了。無法回答的條件現在一律為 false
  - 運算子的定位改在插值**之前**，含有 `==` 或 `>` 的值（API 回應、網址）不會再把算式切在錯的地方
  - `interpolate()` 改成一次 regex 掃過整個模板，不再一個變數一個變數替換——那會讓結果取決於 map 的迭代順序，也會去替換掉本來就屬於某個值的一部分的 `{{…}}`
- **音量改存百分比**：滑桿原本寫死 0..15 並當成絕對階數寫入。實機上媒體有 30 階、鈴聲只有 7 階，於是要求 15 在媒體上只有一半音量，鈴聲超過 8 的部分整段是死的；同一個流程在 15 階的手機上卻是最大聲。流程會被匯出、匯入、由 AI 產生，存一個在每台裝置代表不同響度的數字是錯的。現在滑桿是 0-100%，執行時依這台裝置的範圍換算，滑桿下方會寫出換算結果（「30 階中的第 24 階」）。既有流程照舊執行，只有在使用者下次打開該動作時才轉換
- **關掉選單被當成執行失敗**：`SHOW_MENU` 被關閉時回傳 Failure，於是「改變主意」讓整趟執行標成 FAIL，還把英文字串 `Menu dismissed by user` 原樣寫進記錄給非英文使用者看。現在記為 SKIPPED 且不帶錯誤，並留下 `menu_cancelled` 註記，把「使用者關掉了」和「根本沒跑到那裡」分開
  - 直譯器的四處結果判斷改成「非 Success 就往上傳」，否則 Cancelled 會被 IF／REPEAT／選單吃掉，執行還會繼續往下跑
- **匯入會丟掉流程宣告的變數**：mapper 寫死 `variables = emptyList()`，`.flow` 匯出寫進去的變數在匯入時全被丟棄。真正的傷害不是少一個宣告，而是**安靜地算出錯的結果**——沒有可解析的變數時，`{{counter}} + 1` 會被原樣當成文字存進去
- **釘選到桌面的捷徑用的是舊名字與舊圖示**：`requestPinShortcut` 在同 id 的捷徑已發佈時會忽略除 id 外的所有欄位。現在先推送最新定義再釘選；改名或換圖示也會同步更新已經在桌面上與長按選單裡的捷徑（原本的同步只在引擎服務活著時才跑，總開關關掉時編輯就傳不到）
- **平板與 ChromeOS 上裝不了 github 版**：簡訊與通話權限隱含了電話硬體需求，未宣告為選用時 Play 會視為硬性條件。兩項功能本來就在執行時做權限判斷
- **英文的單複數**：五個把數字插進句子的字串在數量為 1 時都是錯的（「1 tokens used」、「1 minutes」）。改用 plurals；中日文只有單一數量類別，用詞不變
- **小工具**：4x2 預覽在挑選器裡顯示「無法載入小工具」（`previewLayout` 依 RemoteViews 規則展開，而 `Space` 不在允許的 view 清單裡）；預設尺寸改為 4x3、卡片收緊，首次放置就看得完兩列；挑選器預覽的範例文字原本寫死繁體中文，所有英日簡中使用者看到的都是四個中文流程名，現在改由字串資源提供
- **HTTP**：取消執行時 `CancellationException` 被吞成一般失敗、直譯器還繼續跑下一個動作；回應改為串流讀取並限制 256 KB，超過上限直接失敗而不是截成解析不了的 JSON
- **執行記錄的時間戳寫死 `MMM d, HH:mm:ss`**，把英文的欄位順序套到日文與中文上；改為依語系推導

### 變更（Changed）

- **工具鏈與相依套件推到現行 stable**：Kotlin 2.4.10、KSP 2.3.11、Hilt 2.60.1、Room 2.8.4、Compose BOM 2026.08.00、Material 3 1.5.0-alpha26、Coil 3.5.0、Navigation 2.9.8、JUnit 5.14.4 等 26 項。JUnit 6 需要 JVM 17 runtime、而 core 模組釘在 `jvmToolchain(11)`，那是「模組要對到哪個平台」的決定而不是一次版本升級，因此保留在 5.x
  - README 的版本表原本寫著與實際不符的 AGP／Kotlin／Room／Compose BOM 版本，一併更正
- **執行記錄的列距與可點區域**：列高至少 52dp，狀態圖示與標籤拉開，耗時與箭頭之間留出空間，展開的設定區塊也有了像樣的內距
- GitHub issue 模板全面雙語化（中文優先、英文並列），讓非中文使用者也填得完

## [1.3.0] - 2026-08-07

AI 對話從「能用」變成「好用」：回覆逐字出現而不是等半分鐘、可以中途喊停、失敗有得重試、關掉 App 再回來對話還在，而且看得出 AI 這次到底改了流程的哪一行。

### 新增（Added）

- **回覆逐字出現（串流）**：改用 Gemini 的 `streamGenerateContent`（SSE），文字邊產生邊顯示，不再是盯著「思考中」等 10～30 秒後整段跳出來
  - 工具呼叫的中間敘述不會殘留：模型先講一句話再去查 App 清單時，那句暫時的話會被收回，不會和最終答案打架
- **停止生成**：產生中送出鈕變成停止鍵。已經吐出來的部分會留在對話裡並標記「已停止」，不是整段丟掉
- **錯誤可以直接重試**：錯誤泡泡附「重試」，重送剛才那句話而不必重打；重試不會讓同一句話在對話裡出現兩次
- **對話會被保存**：App 被系統回收後重開，對話還在，而且**模型那邊的記憶也一起還原**——只還原畫面訊息會變成「看得到歷史但 AI 失憶」，比重新開始更糟（Room 資料庫升到 v4，純新增資料表）
  - 每個流程各自一段對話，自由對話另外一段；流程被刪除時對應的對話也會清掉
- **流程預覽改成顯示差異**：AI 被要求每次回傳「完整的修訂版流程」，所以改一行也會整份重印。編輯既有流程時，預覽卡現在只標出新增／刪除／修改的那幾列，並寫出改了哪個欄位（例如「音量：8 → 15」）
- **建議提示 chips**：輸入框上方提供可點的開場句（編輯流程與從零建立各一組），送出第一則訊息後自動消失，也可以直接關掉
- **回到最新**：往上翻舊訊息時，畫面不再被新訊息拉走，改為浮出「回到最新」按鈕，按下去滑回底部
- **用量透明**：對話卡展開時顯示這段對話已用掉多少 token（使用者用的是自己的 API key）
- **AI 可以直接指定 App 內的特定畫面**：新增 `search_app_shortcuts` 工具，AI 會查詢該 App 實際公開的捷徑（付款碼、新訊息、已儲存地點之類）並原樣帶回 intent_uri。先前 AI 只能把捷徑欄位留空、要使用者自己到編輯器挑
  - 只提供靜態捷徑，與手動挑選器同一個來源，所以 AI 給的選項和使用者自己找得到的完全一致
  - 需要目標 App 自己的 UI 才能產生的捷徑（`ACTION_CREATE_SHORTCUT`）無法由 AI 代填，改以數量回報，讓 AI 在回覆裡說明為什麼要手動挑
  - 該 App 沒有任何可用捷徑時退回「開啟應用程式」，並在摘要裡講明原因
- **用 Gemini 修改現有流程**：流程詳細頁可直接「用 Gemini 編輯」，AI 會先讀進整個流程再依指示修改，儲存時更新原流程而不是產生一份近乎重複的副本
  - 重新進入同一個流程會保留對話；換一個流程則重新開始（否則模型的歷史仍在描述上一個流程）
- **AI 回覆支援 Markdown**：清單、粗體、行內程式碼等格式會正常呈現，不再顯示成原始符號
- **匯入結果改成逐項檢視**：匯入警告不再是一次倒一整面的純文字（原本超過 5 條還會被截斷），改成像權限精靈那樣一次一項，附進度、說明「發生什麼事／代表什麼／要怎麼處理」
  - 警告若知道是哪個項目出問題，會直接提供「前往修正」開啟該觸發器／限制條件／動作的設定
  - 匯入警告全面在地化（en / zh-TW / zh-CN / ja）。轉換器在無資源的純 Kotlin 模組裡，改成回傳代碼與參數，由 App 端翻譯
  - 從「設定」匯入現在也會顯示結果，先前完全沒有任何回饋

### 修正（Fixed）

- **失敗或中斷的對話回合會污染模型記憶**：網路錯誤、使用者按停止、或修復次數用盡時，那一回合的使用者訊息仍留在送給 Gemini 的歷史裡，導致後續每次請求都帶著連續兩個 user 回合，工具呼叫失敗時甚至會留下沒有答案的 functionCall。現在一個回合要嘛完整留下、要嘛完全不留痕跡（畫面上的訊息照樣保留）
- **AI 查詢已安裝 App 時整個畫面凍住**：`search_installed_apps` 會列舉裝置上所有可啟動的 App 並逐一讀取名稱（每次都要開啟該 App 的資源），數百毫秒的工作卻跑在主執行緒上。改到背景執行緒，與旁邊的捷徑查詢一致
- **AI 回合的其他主執行緒工作**：整份對話歷史的 JSON 編碼、每個串流片段的解碼、流程驗證，以及 debug 版把 request／response 切段寫進 log，全部都在主執行緒。整個回合移到背景執行緒
- **串流時每個片段都強制同步重新測量版面**：改用 `requestScrollToItem` 排進下一次測量，不再每秒觸發十幾次強制重排
- **對話捲動會搶走使用者的位置**：只有原本就在底部時才自動跟隨；往上翻閱時不再被新訊息拉走。鍵盤打開時也會重新對齊，最新一則不會被推到看不見的地方
- **全域變數「新增變數」不會自動聚焦**：對話框開啟後鍵盤沒有跳出來，要多點一次輸入框。與新增流程對話框採用同一套處理
- **「關於」頁的隱私權政策與服務條款連結是 404**：連到 `blob/main/`，但本專案的預設分支是 `master`
- **MacroDroid 匯入實際上完全不會生效**（#6）：以下三件事任一都足以讓匯入失效，三件都存在
  - 自動判斷格式時用「內容開頭是 `{`」決定走哪個解析器，但 `.mdr` 本身就是 JSON，於是每個 MacroDroid 檔都被丟去 `.flow` 解析器並回報 parse failed
  - 解析器的欄位名稱與真實格式不符（真實格式為 `macroList` / `m_actionList` / `m_classType`，且設定欄位**平鋪**在物件上、沒有 `options` 子物件），套用在真實檔案上會得到零個巨集
  - 對照表 25 筆動作裡有 20 筆的 class 名稱在 MacroDroid 中根本不存在（`WaitAction` 實為 `PauseAction`、`LaunchApplication` 實為 `LaunchActivityAction`、`MediaAction` 實為 `ControlMediaAction`、`GeoFenceTrigger` 實為 `GeofenceTrigger`…）
- **匯入後設定欄位是空的**（#6）：新增設定欄位（option key）對照層，涵蓋 46 種 MacroDroid class，時間、電量、Wi-Fi、藍牙、通知、簡訊、延遲、音樂、變數、寫入檔案、HTTP 等都會連同設定一起轉換（例如 `m_hour`/`m_minute`/`m_daysOfWeek` → `time`/`repeat`/`days`）
  - 轉不過去的欄位會**逐項寫進匯入警告**並指出原因，不再靜默丟掉
  - 未知的動作型別在匯入後會降級成吐司，現在該吐司會寫明原本是哪個 MacroDroid 動作，而不是一個空吐司
- 動作在 MacroDroid 中被停用（`m_isDisabled`）時，匯入後維持停用

### 變更（Changed）

- **對話版面重做**：流程對話的標頭改為固定在畫面上，不再隨對話捲走——對話很長時關閉與新對話不會再變成找不到的按鈕
  - 標頭會在使用者開始閱讀或輸入時自動收合成一行（拖曳對話、點擊對話區、或點進輸入框），點一下可再展開
  - 兩顆動作鈕加上文字標籤：一個對話框圖示放在流程頭像下方，看不出來是「開新對話」
  - 對話泡泡加上左右留白，雙方訊息不再各自貼齊兩側；留白會隨字級放大而縮小，把寬度還給文字
- **編輯流程時按「新對話」不再變成自由對話**：改為重新開始同一個流程的對話。標頭寫著在編輯哪個流程，卻悄悄把使用者移出那個流程，等於是個陷阱
- **編輯流程時儲存不再自動跳離對話**：改為留在對話並顯示「已更新流程」與「查看流程」動作，方便連續修改；從零新建流程時維持原本的自動跳轉
- **輸入框鍵盤附送出鍵**（`ImeAction.Send`）
- **長對話會自動裁切歷史**：超過一定長度後丟棄最舊的回合，避免每次請求的 token 成本無限成長。流程內容的前導說明永遠保留，也不會把工具呼叫與它的回應拆散
- **無障礙**：最新一則 AI 回覆會被朗讀一次（`liveRegion`），串流中的泡泡刻意不加——高頻更新的內容加上去會變成連續朗讀
- **時間戳分組**：同一方兩分鐘內的連續訊息只在最後一則顯示時間
- domain → `FlowJson` 的轉換抽成共用函式，流程匯出與 AI 差異比對不再各寫一份
- **隱私權政策補上 AI 助手章節**（[PRIVACY.md](PRIVACY.md) §1.3）：原本的政策寫著「不與任何第三方共享您的資料」，但 AI 功能會把對話內容、**整個流程的設定值**（可能含 HTTP 權杖、電話號碼）與比對到的已安裝 App 名稱送給 Google Gemini。現在逐項列出傳送什麼、何時傳送、傳給誰，並提醒機密設定值的風險與停用方式
- MacroDroid 相容範圍擴充至 22 種觸發、30 種動作、7 種條件的 class type（#3、#4、#5、#7）
  - 新增：飛航模式、更換桌布、擴音、啟動捷徑（#3）
  - 新增：搖晃、環境光線觸發（#4）。搖晃靈敏度是 MacroDroid 的全域設定、不存在於巨集裡，因此沿用 NexFlow 預設值
  - 新增：模擬點擊／滑動（#5）。舊檔為 `TouchScreenAction`，新檔為 `UIInteractionAction` 並以 `uiInteractionConfiguration.type`（`Click`／`Gesture`）區分。**照樣對照**以保留座標，並固定附上「僅 GitHub 版可執行」的警告 —— Play 版依平台政策沒有執行器，執行時會失敗並在執行記錄寫明原因
  - 新增：選單（#7）。MacroDroid 的 `OptionDialogAction` 會展開成 `SHOW_MENU` + N×`MENU_CASE` + `END_MENU` 並重算 order；各選項的動作在 MacroDroid 是指向**另一個巨集**（`m_actionMacroGuids`）而非內嵌分支，因此 case 內容必為空，匯入警告會明講
- 支援 `.macro`（單一巨集分享）格式，先前只處理整份備份的 `.mdr`
- 新增 [docs/MACRODROID_IMPORT.md](docs/MACRODROID_IMPORT.md)：記錄真實檔案格式、每一筆對照的查證來源，以及已知一定轉不過來的項目


## [1.2.0] - 2026-08-03

觸發系統補完：觸發器現在會把「發生了什麼」交給流程使用，限制條件真正生效，`ALL` 觸發邏輯不再是空殼，通知也能把使用者直接送到下一個 App。

### 新增（Added）

- **觸發器變數 `{{trigger.x}}`**：觸發事件的內容可直接用在任何條件與動作欄位，編輯器的插入變數選單會列出目前流程可用的名稱
  - 每次執行都有 `trigger.type`、`trigger.timestamp`（手動執行也有）
  - 簡訊 `sender`/`body`、通知 `package`/`title`/`text`、來電 `number`、App 啟動 `package`、Wi-Fi `ssid`/`event`、藍牙 `device`/`event`、電池 `level`/`charging`、NFC `tag_id`、地理圍欄 `event`/`lat`/`lng`、環境光 `lux`、螢幕與耳機 `event`、時間 `time`
  - 平台不給的值會是空字串（例如沒有定位權限時的 Wi-Fi 名稱），而不是整個欄位消失
- **限制條件（Conditions）**：流程編輯器新增「限制條件」區段，觸發器決定「何時」，限制條件決定「要不要」
  - 8 種條件：時間範圍（可跨午夜）、星期、電池電量、充電狀態、Wi-Fi、藍牙音訊裝置、螢幕開關、運算式（與 If 相同語法，可比較觸發器變數與全域變數）
  - 每個條件都可反轉（NOT）；全部成立才執行
  - 不成立時記錄為「已跳過」並附上原因；手動執行被擋下時會顯示提示，不再毫無反應
- **通知／簡訊內容過濾**：關鍵字 + 比對範圍（標題／內文／兩者）+ 比對方式（包含／完全相同／正規表示式）；空白代表不過濾
- **`ALL` 觸發邏輯**：所有觸發器都要在 5 分鐘內響應，流程才會執行
  - 各觸發器回報的變數會合併，後響的覆蓋先響的
  - 刪除觸發器後舊記錄立即失效；手動執行不受 `ALL` 影響
  - 觸發區會顯示時間窗說明，避免「選了 ALL 卻好像壞掉」
- **可跳轉的通知**：通知動作新增「點擊通知時」——開啟 App、開啟連結（含 deep link）、開啟 App 捷徑
  - 適合「先做這件事，之後有空再點通知接續下一件事」的流程，使用者的點擊就是觸發時機
  - 目標 App 未安裝會在權限精靈提早提示；執行時仍會發出通知，但該次執行記為失敗並寫明原因
- **流程圖示**：改用完整 Material Symbols 圖示庫並支援搜尋；桌面捷徑（釘選與長按選單）改用流程自己的圖示與顏色

### 變更（Changed）

- 版本號 1.1.1 → 1.2.0（versionCode 5 → 6）
- 通知監聽保留標題與內文兩個欄位（先前合併成單一字串，無法分別比對）
- 權限檢查涵蓋限制條件（Wi-Fi 名稱需定位、藍牙需連線權限）與通知的點擊目標 App
- 設定欄位支援條件式顯示，AI 目錄會一併告知模型「某欄位僅在 x=y 時適用」
- 運算式求值抽成共用的 `ExpressionEvaluator`，If 動作與運算式條件不會再各自漂移
- 匯入警告改寫：條件現在會被執行，未支援的條件型別會擋住流程；MacroDroid 的限制條件只帶型別、設定需重新填寫
- `docs/FLOW_SCHEMA.md` 補上條件型別、觸發器變數、`ALL` 語意與通知點擊目標

### 修正（Fixed）

- **`ALL` 觸發邏輯從未實作**：UI 可切換、schema 與資料庫也有欄位，但引擎完全不理會——選 `ALL` 實際得到的是 `ANY`
- **限制條件從未執行**：`conditions` 會存檔、匯出、匯入，執行時卻被忽略，流程照跑
- **觸發事件資料全數遺失**：`TriggerEvent.metadata` 在引擎被丟棄，且除了電量以外沒有任何觸發器填寫——「收到訊息後轉發內容」這類流程根本寫不出來
- 通知觸發只能挑 App，無法比對標題或內文；簡訊觸發只能比對寄件者
- 釘選捷徑與長按選單捷徑共用同一組定義，避免其中一方覆寫另一方的圖示與標籤

## [1.1.1] - 2026-07-16

- 無障礙服務說明補上第三項自動化用途（偵測前景 App 以觸發「App 啟動」）
- AI 目錄測試補強

## [1.1.0] - 2026-07-12

新增 Gemini AI 助手、四個新的觸發／動作，以及多項執行透明度與安全性強化。

### 新增（Added）

- **Gemini AI 助手**：用文字或語音描述需求，AI 透過 function calling 產生完整流程草稿供檢視、儲存
  - 自備 Google AI Studio 的 Gemini API 金鑰（設定 → AI），可從線上模型清單挑選模型（自動過濾已淘汰與非對話模型），未選時預設 `gemini-3.5-flash`
  - 對話式建立：AI 會先追問釐清、呼叫「搜尋已安裝 App」工具解析套件名，再產生流程
  - 執行進度即時顯示（聯絡 Gemini／搜尋 App／產生流程／修正錯誤）
  - 系統語音辨識口述輸入，聆聽時輸入框有 Gemini 風格漸層動效
  - 同一對話可繼續要求微調並更新同一個流程，或開新對話；對話與草稿跨頁面保留
  - 訊息可長按複製／編輯，顯示時間戳
- **觸發條件**：搖晃（加速度計，可調靈敏度）、環境光線（lux 門檻，事件式）
- **動作**：模擬點擊（無障礙 `dispatchGesture`，僅 `github` 版）、擴音（通話音訊切至擴音器）
- **執行回饋**：流程執行時顯示 Toast「NexFlow ▶ 流程名稱」，背景觸發不再無聲執行（可於設定關閉）
- **缺少 App 偵測**：流程引用未安裝的 App（分享／匯入或事後解除安裝）時，權限精靈會提示並提供「安裝」直達 Play 商店

### 變更（Changed）

- 版本號 1.0.2 → 1.1.0（versionCode 3 → 4）
- 匯入路徑的 `FlowJson → 領域模型` 轉換抽為共用 mapper，供匯入與 AI 助手共用
- AI 對話介面全面套用 Material 3 Expressive（藥丸形輸入框、`LoadingIndicator`、動態進場、動效 token）

### 修正（Fixed）

- **APP_LAUNCH 觸發失效**：無障礙服務設定漏宣告 `accessibilityEventTypes`，導致從未收到視窗切換事件；補上 `typeWindowStateChanged` 後「App 啟動」觸發恢復運作
- AI 頁進場過場動畫太快幾乎不可見，改為較慢、從右上角 sparkle 展開的縮放
- 語音聽寫／編輯回填時輸入框游標不跟隨文字，改用 `TextFieldValue` 修正
- AI 輸入框與麥克風／送出按鈕高度不對齊

### 安全性（Security）

- **API 金鑰備份外洩（API 30）**：金鑰所在的 `nexflow_ai.xml` 先前僅在 API 31+ 的 `data_extraction_rules.xml` 排除備份；補上 `backup_rules.xml`（API 30 使用）的排除規則，Android 11 裝置的金鑰也不再上傳雲端備份
- Gemini 金鑰只透過 `x-goog-api-key` 標頭傳送、從不寫入 log；除錯用的完整請求／回應內容僅在 `BuildConfig.DEBUG` 下輸出
- AI 產生的流程一律以**停用**狀態加入，須經檢視與授權（走既有權限精靈）才能啟用；AI 只能組出使用者本就能手動設定的流程，無法越權
- Gemini 400 錯誤不再一律誤判為「金鑰無效」，改依錯誤內容分類並顯示伺服器原始訊息
- AI 話題邊界：僅協助自動化流程，無關內容會被婉拒

### Play 版差異（Play flavor）

- 模擬點擊（`SIMULATE_TAP`）僅 `github` 版提供——`dispatchGesture` 會擴大 Play 無障礙用途聲明範圍，故 `play` 版隱藏
- AI catalog 與流程驗證同步套用 flavor 過濾：`play` 版的 AI 不會產生被隱藏的觸發／動作（如簡訊、撥號）

## [1.0.2] - 2026 上半年

首個公開版本基礎。

### 新增

- 「當…→ 就…」流程建立器，14 種觸發 × 25 種動作，支援變數、If/Else、Repeat、Show Menu
- 逐步權限設置向導（含特殊權限與背景定位揭露）
- 首次啟動引導頁
- 更換桌布動作
- TIME 觸發改用 AlarmManager（可跨 Doze 與服務重啟）
- 地理圍欄觸發強化錯誤處理，避免單一觸發失敗導致引擎崩潰
- 多語言：繁體中文、簡體中文、日文、英文
- 匯入／匯出 `.flow`（JSON）與 MacroDroid `.mdr` 相容解析

[1.5.0]: https://github.com/ADSFAaron/NexFlow/releases/tag/v1.5.0
[1.4.0]: https://github.com/ADSFAaron/NexFlow/releases/tag/v1.4.0
[1.3.0]: https://github.com/ADSFAaron/NexFlow/releases/tag/v1.3.0
[1.2.0]: https://github.com/ADSFAaron/NexFlow/releases/tag/v1.2.0
[1.1.1]: https://github.com/ADSFAaron/NexFlow/releases/tag/v1.1.1
[1.1.0]: https://github.com/ADSFAaron/NexFlow/releases/tag/v1.1.0
[1.0.2]: https://github.com/ADSFAaron/NexFlow/releases/tag/v1.0.2
